from setuptools import setup

setup(name='clip',
      version='0.1.0',
      description='Manage the clipboard',
      author='Árni Dagur',
      author_email='arni@dagur.eu',
      scripts=['clip'],
      install_requires=['pyperclip'])
